# Copy this file to .streamlit/secrets.toml for local cloud-database testing.
# This real secrets file is ignored by Git and must never be committed.
app_password = "replace-with-your-private-password"
database_url = "postgresql://replace-with-the-supabase-session-pooler-url"
